using EasySaveProject.Helpers;

class Program
{
    static void Main(string[] args)
    {
        var app = new App();

        // if (args.Length == 0)
        // {
        app.Run();
        //     return;
        // }

        // app.RunCli(args[0]);
    }
}